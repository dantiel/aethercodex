# Settings

projectDirectory      = "$CWD"
# windowTitle         = "$TM_DISPLAYNAME — ${CWD/^.*\///}"
spellingLanguage = "en_GB"


# Includes

includeFiles          = "{$includeFiles,.aethercodex}"
includeInBrowser      = "{$includeInBrowser,.tm-ai}"
includeDirectories    = "{$includeDirectories}"
includeInFileChooser  = "{$includeInFileChooser,.gitignore,*.orig,*.rej,*.js,.env*}"
includeInFolderSearch = "{$includeInFolderSearch}"
includeInArgonaut     = "{}"


# Excludes

excludeFiles          = "{$excludeFiles,destroy,tmtags,tmtagsHistory,tags,.htaccess,dump.rdb,*rid,*lock,*log,*.map,config.codekit}"
excludeInBrowser      = "{$excludeInBrowser,*.orig,*.rej,*.js}"
excludeDirectories    = "{$excludeDirectories,excludedDir1}"
excludeInFileChooser  = "{$excludeInFileChooser}"
excludeInFolderSearch = "{$excludeInFolderSearch,.bundle,.vendor_bundle}"
excludeInArgonaut     = "{$excludeInBrowser}"

# Variables

TM_ORGANIZATION_NAME  = 'darographix'

IOSEVKA_CURLY_FONT    = 'Iosevka-Curly-Light'
INPUT_MONO_COMPRESSED = 'InputMonoCompressed-ExtraLight'

fontName              = $IOSEVKA_CURLY_FONT

[ source.ruby ]
fontName              = $INPUT_MONO_COMPRESSED
fontName              = $IOSEVKA_CURLY_FONT

